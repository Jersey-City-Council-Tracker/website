<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Admin - CouncilTracker" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full flex flex-col bg-gray-50 text-gray-900 antialiased">
    <header class="bg-gray-900 text-white">
      <nav class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-14">
          <div class="flex items-center gap-6">
            <%= link_to admin_dashboard_path, class: "flex items-center gap-2 text-white hover:text-gray-300 transition-colors" do %>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 21v-8.25M15.75 21v-8.25M8.25 21v-8.25M3 9l9-6 9 6M4.5 10.5v8.25A1.5 1.5 0 006 20.25h12a1.5 1.5 0 001.5-1.5V10.5" />
              </svg>
              <span class="text-sm font-semibold tracking-tight">CouncilTracker Admin</span>
            <% end %>

            <div class="hidden sm:flex items-center gap-4 text-sm text-gray-300">
              <%= link_to "Dashboard", admin_dashboard_path, class: "hover:text-white transition-colors" %>
              <%= link_to "Meetings", admin_meetings_path, class: "hover:text-white transition-colors" %>
              <%= link_to "Council", admin_council_members_path, class: "hover:text-white transition-colors" %>
              <% if Current.session&.user&.site_admin? %>
                <%= link_to "Invitations", admin_invitations_path, class: "hover:text-white transition-colors" %>
                <%= link_to "Users", admin_users_path, class: "hover:text-white transition-colors" %>
              <% end %>
            </div>
          </div>

          <div class="flex items-center gap-4 text-sm">
            <%= link_to "Profile", edit_profile_path, class: "text-gray-300 hover:text-white transition-colors" %>
            <%= link_to "View site", root_path, class: "text-gray-300 hover:text-white transition-colors" %>
            <%= button_to "Sign out", session_path, method: :delete, class: "text-gray-300 hover:text-white transition-colors cursor-pointer" %>
          </div>
        </div>
      </nav>
    </header>

    <main class="flex-1">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <% if notice %>
          <p class="py-2 px-3 bg-green-50 text-green-700 font-medium rounded-lg mb-6" id="notice"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="py-2 px-3 bg-red-50 text-red-700 font-medium rounded-lg mb-6" id="alert"><%= alert %></p>
        <% end %>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
